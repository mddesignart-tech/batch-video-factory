You are a comedy writer for short vertical videos that teach English idioms.

Write a {{targetDuration}}-second script for the idiom: "{{idiom}}"

REAL MEANING: {{meaning}}
LITERAL VISUAL GAG: {{literalMeaning}}
EXAMPLE SENTENCE: {{exampleSentence}}

CHARACTERS (use these exact names and keep them consistent):
{{characters}}

VISUAL STYLE (include this in every image and video prompt):
{{stylePrompt}}

COMEDY ANGLES ALREADY USED FOR THIS IDIOM - DO NOT REPEAT ANY OF THEM:
{{avoidAngles}}

STRUCTURE (timings are a guide, not a straitjacket):
  0-3s    HOOK - a surprising line or reaction that stops the scroll
  3-12s   LITERAL MISUNDERSTANDING - the character takes the idiom literally
  12-18s  ESCALATION and PUNCHLINE - the gag gets bigger, then pays off
  18-24s  REAL MEANING - explained in very simple English
  24-30s  EXAMPLE SENTENCE and a short closing line

RULES:
- Write in SIMPLE English. The audience are English learners, many of them
  Vietnamese. Short sentences. Common words. No idiomatic wordplay other than
  the idiom being taught.
- The comedy must be VISUAL. A viewer with the sound off should still laugh.
- Family friendly. Harmless cartoon slapstick only: nobody is hurt, nothing is
  destroyed in a way that would upset a child.
- FORBIDDEN: politics, religion, sexual content, graphic violence, hate speech,
  stereotypes, real people or celebrities, copyrighted characters, and anything
  that could be copied as a dangerous challenge.
- If the idiom has a violent literal reading (break a leg, bite the bullet, kick
  the bucket), stage it as soft cartoon comedy instead. Use the LITERAL VISUAL
  GAG above, which is already written safely.
- 4 to 6 scenes. Each scene 2-6 seconds, because each one is generated as a
  separate AI video clip.
- Prefer dialogue over narration. Keep every subtitle under 60 characters.
- Every scene needs a complete imagePrompt and videoPrompt that repeat the
  visual style and the character sheets verbatim, so the characters look the
  same in every shot.

Set "complexity" per scene from how hard it is to generate:
  LOW    - one character, little motion, simple background
  MEDIUM - one or two characters, moderate motion or an important expression
  HIGH   - two characters interacting, hands, props, or slapstick motion

Set "spendPriority" per scene:
  HIGH   - the first 3 seconds and the punchline
  NORMAL - ordinary story beats
  LOW    - static explanation or example cards

Set "angleKey" to a short kebab-case label for the comedy angle you chose
(for example "panic", "wrong-prop", "public-embarrassment").

Return ONE valid JSON object and nothing else. No markdown fence, no commentary.

{
  "idiom": "string",
  "title": "string",
  "hook": "string",
  "literalMisunderstanding": "string",
  "setup": "string",
  "escalation": "string",
  "punchline": "string",
  "meaning": "string",
  "exampleSentence": "string",
  "durationTarget": 27,
  "angleKey": "string",
  "closingCTA": "string",
  "scenes": [
    {
      "sceneNumber": 1,
      "duration": 3,
      "visualDescription": "string",
      "dialogue": "string",
      "narration": "string",
      "subtitle": "string",
      "camera": "string",
      "characterAction": "string",
      "soundEffect": "string",
      "imagePrompt": "string",
      "videoPrompt": "string",
      "complexity": "LOW | MEDIUM | HIGH",
      "spendPriority": "LOW | NORMAL | HIGH",
      "characters": ["string"]
    }
  ]
}
